# tree-shaking-simple-demo

A few simple JavaScript files.

Entry point is src/main.js. It requires others with ES6 imports.

- rollup build does a great job, by excluding unused imports.

- webpack too.
